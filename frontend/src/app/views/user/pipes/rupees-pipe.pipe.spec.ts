import { RupeesPipePipe } from './rupees-pipe.pipe';

describe('RupeesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RupeesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
